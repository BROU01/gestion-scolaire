# === SERVEUR ===
PORT=3001
NODE_ENV=development

# === SÉCURITÉ ===
JWT_SECRET=gestion-scolaire-secret-key-2025
JWT_EXPIRES_IN=24h
CORS_ORIGIN=*
RATE_LIMIT_MAX=100

# === BDD (SQLite locale) ===
# Pas de config nécessaire — utilise server/gestion_scolaire.db
